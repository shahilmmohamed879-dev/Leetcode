class Solution(object):
    def gcdOfStrings(self, str1, str2):
        if str1 + str2 != str2 + str1:
            return ""

        m = len(str1)
        n = len(str2)

       
        while n != 0:
            m,n = n, m % n

        return str1[:m]
